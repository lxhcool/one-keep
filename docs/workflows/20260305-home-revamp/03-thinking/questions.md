# 思考审查 - 疑问清单

| 编号 | 级别 | 类别 | 问题描述 | 默认答复（已确认） | 状态 |
| --- | --- | --- | --- | --- | --- |
| Q1 | 高 | 交互 | 月份选择器最终形态（下拉/弹层/独立页）未定 | 使用弹层选择器 | 已确认 |
| Q2 | 高 | 业务规则 | 流水金额符号规则是否固定（支出负数、收入正数） | 收入正数、支出负数 | 已确认 |
| Q3 | 中 | 交互 | 中间“记账”说明位点击是否需要轻反馈（如无波纹/仅震动） | 不可点击且无点击反馈 | 已确认 |
| Q4 | 中 | 数据 | 当月统计和流水列表是否允许分步返回（先统计后列表） | 允许分步返回 | 已确认 |
| Q5 | 中 | 异常 | 请求失败文案与重试次数策略是否有统一规范 | 可无限重试，前端节流 1 秒 | 已确认 |
| Q6 | 低 | 视觉 | 空状态插图与文案是否已有品牌规范素材 | 先用占位素材，后补品牌素材 | 已确认 |

## 结论

- 以上默认方案由你于 2026-03-05 通过“默认”统一确认。
- 阶段 3 阻塞项已清零，可进入阶段 4 架构设计。
