{
  "name": "one-keep",
  "version": "0.1.0",
  "private": true,
  "description": "OneKeep Flutter client scaffold",
  "scripts": {
    "dev:client": "cd client && flutter run",
    "analyze:client": "cd client && flutter analyze",
    "test:client": "cd client && flutter test"
  }
}
