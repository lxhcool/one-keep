# Database — 本地开发用 sqlite，生产用 mysql
# 本地: DB_PROVIDER="sqlite"  DATABASE_URL="file:./dev.db"
# 生产: DB_PROVIDER="mysql"   DATABASE_URL="mysql://user:pass@localhost:3306/one_keep"
DB_PROVIDER="sqlite"
DATABASE_URL="file:./dev.db"

# JWT — 生产环境请更换为随机字符串
JWT_SECRET="cd843baeac34a39a7e7f7662cdb4055908aebe208262e7662dd189469e83d168"

# Resend — 邮箱验证码 (https://resend.com)
# 域名: lxhcoool.cn (已验证)
# 不填则不发送邮件，验证码会打印在服务端日志中
RESEND_API_KEY=""
RESEND_FROM="厘清 <noreply@lxhcoool.cn>"

# Server
PORT=3002
HOST="0.0.0.0"
